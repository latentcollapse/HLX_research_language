## Gemini Added Memories
- The user views HLX (Runic/Visual) as optimized for LLMs (token efficiency/compression) and HLXL (Linear/ASCII) as optimized for humans. They see the Latent Space (LS) layer as an intermediate compression pipe for transferring data between formats.
- HLX-A is now JIT compiled via LLVM ("Iron" tier), supporting native math, logic, and SDL2 graphics.
- HLX Iron Backend (LLVM JIT) with SDL2 support is implemented and verified. SDL2 initialization requires a GUI environment.
- I must update both ../.gemini/GEMINI.md and _docs/GEMINI.md whenever I add new memories to ensure the documentation stays in sync.
- Phase 4 (Ouroboros) is underway. The Rust bootstrap compiler now supports HLX-A with break/continue, string escapes, object literals, field access, and optional 'if' parentheses.
- The self-hosted compiler (compiler.hlxc) successfully parses up to the final 'main' function, with lowering implemented for all major language features.
- Infrastructure for Ouroboros is complete. Added `hlx run --output` and `hlx build-crate` to act as the "linker" for the self-hosted compiler.
- Fixed `print` instruction lowering to handle multiple arguments, matching the self-hosted compiler's usage.
- Stage 1 compiler (compiled by Rust) is functional and successfully executes on the VM.
- **CRITICAL MILESTONE REACHED: OUROBOROS COMPLETE.** (2026-01-05)
- The HLX compiler is now fully self-hosted. The Rust bootstrap compiler produced Stage 1, which successfully compiled itself to produce Stage 2. Stage 2 then compiled itself to produce Stage 3.
- Determinism verified: Stage 2 and Stage 3 hashes match bit-perfectly (5b8fa2ee...).
- We are pivoting to building "HLX-Flow" (an n8n killer) to demonstrate the language's power.