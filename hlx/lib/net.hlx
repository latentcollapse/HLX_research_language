// HLX Network Standard Library

fn get(url) {
    return http_request("GET", url, null, {});
}

fn post(url, body) {
    return http_request("POST", url, body, {});
}

fn request(method, url, body, headers) {
    return http_request(method, url, body, headers);
}
