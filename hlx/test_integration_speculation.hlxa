program test_speculation {

@swarm(size=4)
fn parallel_compute(x: Int) -> Int {
    let step1 = x * 2;
    barrier("computation_phase1");
    let step2 = step1 + 10;
    barrier("computation_phase2");
    return step2;
}

fn main() -> Int {
    return parallel_compute(5);
}

}
