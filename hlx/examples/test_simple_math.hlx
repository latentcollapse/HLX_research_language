program test_simple {
    fn main() {
        print("Testing sqrt(16):");
        let x = 16;
        let guess = x / 2;
        let i = 0;
        loop(i < 5, 5) {
            guess = (guess + (x / guess)) / 2;
            i = i + 1;
        }
        print(guess);
        return 0;
    }
}
