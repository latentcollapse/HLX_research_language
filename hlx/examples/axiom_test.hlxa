program axiom_test {
    fn main() {
        // Deterministic arithmetic
        let a = 7;
        let b = 3;
        let mul = a * b;
        let div = a / b;
        let result = mul + div;
        return result;
    }
}
