fn test(st) -> any {
    return st;
}

fn main() -> any {
    let s = {"insts": "Success"};
    s = test(s);
    // If test() returns the frame (containing 'st'), s.insts will fail.
    // If test() returns 'st', s.insts will be "Success".
    print(s.insts);
    return 0;
}
