[package]
name = "hlx-llvm"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "LLVM backend for HLX - native code generation"

[lib]
path = "mod.rs"

[dependencies]
inkwell = { version = "0.5", features = ["llvm18-0"] }
