// APE main.rs — binary target removed during Structural Refactor.
// APE is now a library-only crate (SQLite model).
// Use `ape::ConscienceKernel` from your Rust code or `from ape import ConscienceKernel` in Python.
fn main() {
    eprintln!("APE is a library — use ape::ConscienceKernel from your code.");
    eprintln!("Example: let mut kernel = ape::conscience::ConscienceKernel::new();");
    std::process::exit(1);
}
