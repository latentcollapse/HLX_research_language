//! Module system (experimental)
//!
//! Module resolution for APE. Currently stubbed — requires new approach
//! without the deleted lexer/parser.

/// Placeholder for future module system
pub struct ModuleResolver;

impl ModuleResolver {
    pub fn new() -> Self {
        ModuleResolver
    }
}
