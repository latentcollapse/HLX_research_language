//! DSF — Dumb Shit Filter (experimental)
//!
//! Static analysis for common AI agent mistakes.
//! Currently stubbed — requires AST visitor rewrite to consume HLX AST.

/// Placeholder for future DSF rewrite as HLX AST visitor
pub struct DsfChecker;

impl DsfChecker {
    pub fn new() -> Self {
        DsfChecker
    }
}
